.media[album]
  = link_to album, class: "pull-left thumbnail" do
    - if album.images.first
      = image_tag album.images.first.image.url(:thumb), class: "media-object"
    - else
      = image_tag "no_photo.png", class: "media-object"
  = button_tag class: "remove-album close hide", id: album.id do
    &times;
  = button_tag class: "edit-album close hide", id: album.id do
    %i.icon-pencil
  .media-body
    %h4.media-heading
      = link_to album.name, album
    %div
      = album.desc
