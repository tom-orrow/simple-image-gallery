%li.span2
  = link_to image.image.url, id: image.id, class: "thumbnail", data: { gallery: "gallery" } do
    = image_tag image.image.url(:thumb)
  = button_tag class: "close hide", id: image.id do
    &times;

